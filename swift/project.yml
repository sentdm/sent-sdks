name: Sentdm
targets:
  Sentdm:
    type: framework
    platform: iOS
    deploymentTarget: "13.0"
    sources: [Sentdm]
    info:
      path: ./Info.plist
      version: 1.0.0
    settings:
      APPLICATION_EXTENSION_API_ONLY: true
    scheme: {}
    dependencies:
      - carthage: AnyCodable